# Base URL of the ugra-back FastAPI server (includes the /api prefix)
VITE_API_BASE_URL=http://localhost:8000/api

# Origin of the backend (used to build absolute image URLs served from /api/items/{id}/image)
VITE_API_ORIGIN=http://localhost:8000
